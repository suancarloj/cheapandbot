{
  "name": "hackathon2016",
  "version": "1.0.0",
  "description": "Cheap@ndBot",
  "main": "build/app.js",
  "scripts": {
    "start": "npm run build & nodemon -w ./build ./build/app.js",
    "build": "babel src -d build --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Bot",
    "cheap",
    "deals"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": "6.x.x"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-latest": "^6.16.0",
    "nodemon": "^1.11.0"
  },
  "dependencies": {
    "botbuilder": "^3.3.3",
    "cloudconvert": "^1.1.1",
    "request": "^2.75.0",
    "restify": "^4.1.1"
  }
}
